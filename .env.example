# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/enterprise"

# JWT Secret
JWT_SECRET=your_jwt_secret_key

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: External API Token (if needed)
API_TOKEN=your_external_api_token